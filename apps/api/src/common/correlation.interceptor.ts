import{CallHandler,ExecutionContext,Injectable,NestInterceptor}from'@nestjs/common';import type{Observable}from'rxjs';import type{Response}from'express';import type{CserRequest}from'./request-context';import{randomUUID}from'node:crypto';
@Injectable()export class CorrelationInterceptor implements NestInterceptor{intercept(context:ExecutionContext,next:CallHandler):Observable<unknown>{const req=context.switchToHttp().getRequest<CserRequest>(),res=context.switchToHttp().getResponse<Response>();req.correlationId=String(req.headers['x-correlation-id']??randomUUID());res.setHeader('x-correlation-id',req.correlationId);return next.handle()}}
