import{Controller,Get}from'@nestjs/common';import{PrismaService}from'../prisma/prisma.service';@Controller()export class HealthController{constructor(private readonly prisma:PrismaService){}@Get('health/live')live(){return{status:'ok',service:'cser-api',time:new Date().toISOString()}}@Get('health/ready')async ready(){await this.prisma.$queryRaw`SELECT 1`;return{status:'ready',database:'postgresql'}}@Get('metrics')async metrics(){const[workloads,findings]=await Promise.all([this.prisma.workload.count(),this.prisma.finding.count()]);return`# TYPE cser_workloads gauge\ncser_workloads ${workloads}\n# TYPE cser_findings gauge\ncser_findings ${findings}\n`}}
