import{useQuery}from'@tanstack/react-query';import type{AuditEvent,Page}from'@cser/contracts';import{api}from'../../api';import{ErrorState,Loading,PageHeader,Panel}from'../../ui';
export function AuditPage(){const q=useQuery({queryKey:['audit'],queryFn:()=>api<Page<AuditEvent>>('/audit?pageSize=100')});return <div className="content"><PageHeader title="Audit Explorer" description="Read-only append-oriented history with actor, tenant, entity, safe diff and correlation context"/>{q.isLoading?<Loading/>:q.error||!q.data?<ErrorState error={q.error}/>:<Panel><div className="table-shell"><table className="semantic-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Correlation ID</th></tr></thead><tbody>{q.data.items.map(a=><tr key={a.id}><td>{new Date(a.createdAt).toLocaleString()}</td><td>{a.actorName}<small className="muted">{a.actorRole}</small></td><td>{a.action}</td><td>{a.entityType} <span className="code">{a.entityId}</span></td><td className="code">{a.correlationId}</td></tr>)}</tbody></table></div></Panel>}</div>}
