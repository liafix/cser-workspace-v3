import{PageHeader,Panel,Badge}from'../../ui';import{useSession}from'../../session';
export function PermissionsPage(){const{session}=useSession();return <div className="content"><PageHeader title="Permissions Inspector" description="Explain effective tenant, role and action scope without relying on hidden UI"/><div className="two-column"><Panel title="Current identity"><div className="definition-grid"><div className="definition"><small>User</small><strong>{session?.user.displayName}</strong></div><div className="definition"><small>Role</small><strong>{session?.role.replaceAll('_',' ')}</strong></div><div className="definition"><small>Tenant</small><strong>{session?.tenant.name}</strong></div><div className="definition"><small>Session mode</small><strong>{session?.demoMode?'Fictional demo':'OIDC adapter'}</strong></div></div></Panel><Panel title="Effective permissions"><div className="detail-meta">{session?.permissions.map(p=><Badge key={p} tone="blue">{p}</Badge>)}</div></Panel></div><Panel title="Why an action may be unavailable"><div className="permission-callout">The API evaluates identity, active membership, tenant ownership, permission, object assignment, state-machine guard, separation of duties, ETag version and idempotency before any protected mutation.</div></Panel></div>}
