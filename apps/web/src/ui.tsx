import type{ReactNode}from'react';
export const PageHeader=({title,description,actions}:{title:string;description:string;actions?:ReactNode})=><header className="page-header"><div><h1>{title}</h1><p>{description}</p></div>{actions&&<div className="page-actions">{actions}</div>}</header>;
export const Panel=({title,subtitle,children,actions,className=''}:{title?:string;subtitle?:string;children:ReactNode;actions?:ReactNode;className?:string})=><section className={`panel ${className}`}>{(title||actions)&&<header className="panel-heading"><div>{title&&<h2>{title}</h2>}{subtitle&&<p>{subtitle}</p>}</div>{actions}</header>}{children}</section>;
export const Loading=({label='Loading data'}:{label?:string})=><div className="loading" role="status"><span className="spinner"/>{label}</div>;
export const ErrorState=({error,retry}:{error:unknown;retry?:()=>void})=><div className="error-state" role="alert"><strong>Unable to load this view</strong><p>{error instanceof Error?error.message:'Unexpected error'}</p>{retry&&<button onClick={retry}>Retry</button>}</div>;
export const Badge=({children,tone='neutral'}:{children:ReactNode;tone?:string})=><span className={`badge badge-${tone.toLowerCase()}`}>{children}</span>;
export function Metric({label,value,detail,onClick,tone='blue'}:{label:string;value:string|number;detail:string;onClick?:()=>void;tone?:string}){const x=<><span>{label}</span><strong>{value}</strong><small>{detail}</small></>;return onClick?<button className={`metric metric-${tone}`} onClick={onClick}>{x}</button>:<div className={`metric metric-${tone}`}>{x}</div>}
