import{describe,expect,it}from'vitest';import{assertTransition,calculateImpact,calculateRisk,DomainError}from'./index';
const base={state:'READY_FOR_REVIEW' as const,severity:'CRITICAL' as const,role:'SECURITY_ANALYST' as const,hasTask:true,hasCleanEvidence:true,checklistComplete:true,remediationAuthorId:'ops',actorId:'analyst',passedVerification:true,blockingTask:false};
describe('domain',()=>{it('blocks critical self verification',()=>expect(()=>assertTransition('verify',{...base,remediationAuthorId:'analyst'})).toThrow(DomainError));it('scores transparently',()=>expect(calculateRisk({severity:'CRITICAL',confidence:'HIGH',production:true,internetExposed:true,unprotected:true,criticalAsset:true,overdue:false,compensatingControl:false}).score).toBe(97));it('matches base ROI',()=>expect(calculateImpact({activeOrganizations:150,supportContactsPerOrgPerYear:18,deflectionRate:.2,averageContactHours:.75,loadedSupportHourValue:40,onboardingsPerYear:40,savedHoursPerOnboarding:8,technicalHourValue:55,qualifiedExpansionEvents:8,contributionPerExpansion:5000,retainedCustomers:2,annualContributionPerRetainedCustomer:15000,annualOperatingCost:36000,initialInvestment:120000}).grossValue).toBe(103800))});
