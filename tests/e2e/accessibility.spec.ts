import{test,expect}from'@playwright/test';import AxeBuilder from'@axe-core/playwright';import{loginAs}from'./helpers';
for(const route of ['/','/login'])test(`axe ${route}`,async({page})=>{await page.goto(route);const results=await new AxeBuilder({page}).analyze();expect(results.violations.filter(v=>['serious','critical'].includes(v.impact??''))).toEqual([])});test('axe authenticated core pages',async({page})=>{await loginAs(page,'Sofia Marin');for(const route of ['/app/overview','/app/workloads','/app/findings','/app/analytics/impact']){await page.goto(route);const results=await new AxeBuilder({page}).analyze();expect(results.violations.filter(v=>['serious','critical'].includes(v.impact??'')),route).toEqual([])}});
